import { createTheme } from '@mui/material/styles';

export const theme = createTheme({
  palette: {
    primary: {
      main: '#Ff6984', // Hot pink
    },
    secondary: {
      main: '#FFA07A', // Light salmon
    },
  },
  typography: {
    fontFamily: 'Roboto, Arial, sans-serif',
  },
});
